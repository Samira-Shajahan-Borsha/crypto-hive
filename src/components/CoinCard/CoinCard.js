import React from 'react';

const CoinCard = ({ coin }) => {
    const { name, image, symbol } = coin;
    return (
        <div>
            <h1>Coin card</h1>
        </div>
    );
};

export default CoinCard;